<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tejven.homefood">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DriverRegularFinalActivity"></activity>
        <activity android:name=".DriverFinalActivity" />
        <activity android:name=".RatingActivity" />
        <activity android:name=".UserBookingConfirmed" />
        <activity android:name=".PricingOTPActivity" />
        <activity android:name=".GetBookingsActivity" />
        <activity android:name=".UpdateDriverProfileActivity" />
        <activity android:name=".GetRegularBookingsActivity" />
        <activity android:name=".DriverActivity" />
        <activity android:name=".SelectionActivity" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".UpdateProfileActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>